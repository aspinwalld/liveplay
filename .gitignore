# ---------------------------------------------------------------------------
# Node / Nuxt / Electron (client)
# ---------------------------------------------------------------------------
node_modules/
client/node_modules/
.nuxt/
client/.nuxt/
.output/
client/.output/
dist/
client/dist/
dist-electron/
client/dist-electron/
.env
.DS_Store
*.log
*.local
.vscode/*
!.vscode/extensions.json
.idea
# Lockfiles are committed so CI builds are reproducible. The root lockfile
# covers the client workspace too — client/package-lock.json is never read by
# npm (the workspace prefix resolves to the repo root) and stays ignored.
package-lock.json
!/package-lock.json
!/docs-site/package-lock.json
build/
!client/build/
out/
yt-dlp.exe
.claude

# Reference screenshots and mockups dropped at the repo root while designing.
# Scoped to the root on purpose so genuine assets (client/public/screenshots)
# are unaffected.
/*.jpg
/*.jpeg
/*.png
/*.gif
/*.webp

# ---------------------------------------------------------------------------
# Server (C++ / CMake / vcpkg)
# ---------------------------------------------------------------------------
server/build/
server/build-*/
server/out/
server/cmake-build-*/
server/vcpkg_installed/
server/.vcpkg-root
server/CMakeCache.txt
server/CMakeFiles/
server/compile_commands.json
server/.vs/
